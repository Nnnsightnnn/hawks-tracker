// built by nnnsightnnn — signal from noise
import React from 'react'
import ReactDOM from 'react-dom/client'
import HawksTracker from './App'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <HawksTracker />
  </React.StrictMode>,
)
